<Window x:Class="ConditioningControlPanel.UpdateProgressDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Downloading Update"
        Height="150" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="ToolWindow"
        Background="{DynamicResource DarkerBgBrush}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Text="Downloading Update..."
                   Foreground="{DynamicResource PinkBrush}" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>

        <!-- Progress Bar -->
        <Border Grid.Row="1" Background="{DynamicResource PanelBgBrush}" CornerRadius="4" Height="20" Padding="2">
            <Grid>
                <Border x:Name="ProgressFill" Background="{DynamicResource PinkBrush}" CornerRadius="3" HorizontalAlignment="Left" Width="0"/>
            </Grid>
        </Border>

        <!-- Progress Text -->
        <TextBlock x:Name="TxtProgress" Grid.Row="2"
                   Text="0%"
                   Foreground="#E0E0E0" FontSize="13"
                   HorizontalAlignment="Center" Margin="0,10,0,0"/>
    </Grid>
</Window>
