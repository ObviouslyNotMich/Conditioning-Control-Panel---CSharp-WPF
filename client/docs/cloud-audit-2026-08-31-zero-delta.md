# Cloud audit — 2026-08-31, zero-delta pass + PR backlog flag

Scheduled cloud YAGNI/efficiency pass on `client/src`, run from a Linux sandbox (no `dotnet` SDK, no
headed capture, no gates). Per the hard limit in `client/docs/cloud-audit-brief.md`'s deleted-but-
recovered content: **nothing is landed** — no push to `feat/crossplatform`, no `client/src` change, no
test or pin touched.

## Zero code delta since the last three passes

This run's `feat/crossplatform` HEAD (`1d033c85f`) is byte-identical to the base commit of PR #35,
#36, and #37 — the three most recent prior audit passes. Nothing in `client/src` has changed since
any of them landed their findings:

- **#35** — broad two-agent pass: 423 private methods traced to real call sites (zero dead), six
  single-impl interfaces checked and five excluded as documented seams, one genuine cut found
  (`ILockCardPhrasePool`, `-14 lines`).
- **#36** — filled #35's two named gaps (XAML files read structurally only, several `Features/*`
  subfolders not read line-by-line): seven more cuts found (`-33 lines` total), rejected candidates
  listed with reasoning.
- **#37** — Win32 interop duplication pass: 1023 private methods re-scanned (zero dead), P/Invoke
  duplication across seven interop files identified (`-350 lines` possible, needs a Windows compiler
  to consolidate safely — correctly not attempted from this sandbox), public-surface check confirmed
  no `InternalsVisibleTo` over-exposure.

Those three passes together are a thorough, cross-verified sweep of `client/src`. Re-running the same
sweep against unchanged source would not find anything they didn't already find, so this pass did not
repeat it. Instead it spot-checked five angles none of the three explicitly named — all came back
clean or already-legitimate:

- `.Where(...).Count()` / `.Any()` / `.FirstOrDefault()` chains that collapse to a single predicate
  call — 0 hits.
- `.ToList().ForEach(...)` — 0 hits.
- Empty `catch` blocks — 0 hits.
- `TODO`/`FIXME`/`HACK`/`XXX` markers — 0 hits.
- `string.Format` call sites (3 hits, all `CultureInfo.InvariantCulture` template formatting for
  user-facing gate text — not a clean interpolation swap, left alone).

## Ranked cut-list for this pass

**Nothing new.** The still-open, still-valid, still-unlanded cuts are exactly the ones already
reported in #35 (`ILockCardPhrasePool`, `-14 lines`) and #36 (seven items, `-33 lines`); #37's Win32
P/Invoke consolidation (`-350 lines` possible) needs a Windows build to do safely and is correctly
still unimplemented. Re-stating them here would duplicate, not add to, those PRs.

`Lean already. Ship.` — for anything a Linux sandbox can respond to right now.

## The actual finding: the backlog, not the code

As of this run there are **37 open `audit:` PRs (#1–#37)**, spanning 2026-08-22 through today, and
**none have been merged or closed**. #36 already flagged this nine hours ago (at PR #35 open); the
count has grown by two since with no triage in between. Three consecutive passes (#35, #36, and now
this one) found the source clean or unchanged, which means the 2-hourly cadence is now spending a full
sandbox run mostly re-confirming "nothing new" rather than surfacing anything actionable.

Recommend, as an owner call (not this pass's to make):

1. Merge the small, low-risk, already-verified cuts sitting in #35 and #36 (`-14` and `-33` lines,
   each independently grep-verified against `client/src` and `client/tests`), or close them with a
   reason if they're not wanted.
2. Close the duplicate brief-restoration PRs (#34 and #35 both restore
   `client/docs/cloud-audit-brief.md` verbatim from `1bdf998e4`'s deletion; #36 carries the identical
   restoration as its only diff) — merging any one of the three and closing the rest removes three
   redundant PRs at once.
3. Consider slowing or pausing the 2-hour cadence until the backlog is triaged — a fresh pass against
   unchanged source has diminishing value and is now the majority of the queue.

## Out of scope

No correctness, security, or performance issues noticed in this pass, beyond the two already on record
in #35 (`MediaFoundationCameraCapture.cs:876` O(n²) dedup, bounded; `ScriptedSessionRamp.cs:251`
hand-rolled clamp-then-lerp, not a safe stdlib swap).

---
_Generated by an automated Claude Code cloud audit session._
